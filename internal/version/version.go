package version

const (
	AppName   = "skill-sync-manager"
	RepoOwner = "dirary0"
	RepoName  = "skill-sync-manager"
)

// Version is the running application version. Release builds should override it
// with: -X skill-sync-manager/internal/version.Version=<tag>
var Version = "dev"
